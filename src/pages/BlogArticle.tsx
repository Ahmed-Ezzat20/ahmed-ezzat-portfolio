import { useEffect, useState } from 'react';
import { ArrowLeft, Check, Clock3, Languages, Linkedin, Share2 } from 'lucide-react';
import { Link, Navigate, useParams } from 'react-router-dom';
import { blogPosts } from '@/content/blogPosts';
import { useLanguage } from '@/contexts/LanguageContext';

const BlogArticle = () => {
  const { slug } = useParams();
  const post = blogPosts.find((item) => item.slug === slug);
  const [copied, setCopied] = useState(false);
  const { language, isArabic, setLanguage, tr } = useLanguage();
  const blogUrl = `/blog${isArabic ? '?lang=ar' : ''}`;
  const homeUrl = `/${isArabic ? '?lang=ar' : ''}`;

  useEffect(() => { if (import.meta.env.MODE !== 'test') window.scrollTo(0, 0); if (post) document.title = `${post.title[language]} | Ahmed Ezzat`; }, [slug, post, language]);
  if (!post) return <Navigate to={blogUrl} replace />;

  const copyLink = async () => { try { await navigator.clipboard.writeText(window.location.href); setCopied(true); window.setTimeout(() => setCopied(false), 1800); } catch { window.prompt(tr('Copy this article URL:', 'انسخ رابط المقال:'), window.location.href); } };
  const displayHeading = (heading: string) => isArabic ? 'font-display' : 'font-[Georgia,serif]';

  return <div className="min-h-screen bg-[#fcfcf9] text-[#242424]"><header className="border-b border-black/10 bg-[#fcfcf9]/95 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5"><Link to={blogUrl} className="inline-flex items-center gap-2 text-sm font-medium text-[#555] transition-colors hover:text-[#0b7f95]"><ArrowLeft className={`h-4 w-4 ${isArabic ? 'rotate-180' : ''}`} />{tr('Field Notes', 'ملاحظات ميدانية')}</Link><div className="flex items-center gap-4"><button onClick={() => setLanguage(isArabic ? 'en' : 'ar')} className="inline-flex items-center gap-1.5 rounded-full border border-black/10 px-3 py-1.5 text-xs font-semibold text-[#0b3346] transition-colors hover:border-[#0b7f95] hover:text-[#0b7f95]"><Languages className="h-3.5 w-3.5" />{isArabic ? 'EN' : 'العربية'}</button><Link to={homeUrl} className="font-display text-sm font-bold tracking-[-0.03em] text-[#242424]">Ahmed Ezzat</Link></div></div></header><main className="px-6 pb-20 pt-14 md:pb-28 md:pt-20"><article className="mx-auto max-w-[720px]"><p className="font-mono text-[11px] uppercase tracking-[0.14em] text-[#0b7f95]">{post.category[language]}</p><h1 className={`mt-5 ${displayHeading(post.title[language])} text-4xl leading-[1.15] tracking-[-0.045em] text-[#1c1c1c] md:text-6xl`}>{post.title[language]}</h1><p className={`mt-6 ${isArabic ? 'font-sans' : 'font-[Georgia,serif]'} text-xl leading-8 text-[#666] md:text-2xl md:leading-9`}>{post.excerpt[language]}</p><div className="mt-9 flex items-center justify-between border-y border-black/10 py-5"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-[#0b3346] font-display text-sm font-bold text-cyan-100">AE</span><div><p className="text-sm font-semibold text-[#333]">Ahmed Ezzat</p><p className="mt-0.5 text-xs text-[#777]">{tr('Voice AI Engineer', 'مهندس ذكاء اصطناعي صوتي')} · {post.publishedAt[language]}</p></div></div><div className="flex items-center gap-4 text-xs text-[#777]"><span className="inline-flex items-center gap-1.5"><Clock3 className="h-3.5 w-3.5" />{post.readingTime[language]}</span><button onClick={copyLink} className="inline-flex items-center gap-1.5 rounded-full border border-black/10 px-3 py-1.5 transition-colors hover:border-[#0b7f95] hover:text-[#0b7f95]">{copied ? <Check className="h-3.5 w-3.5" /> : <Share2 className="h-3.5 w-3.5" />}{copied ? tr('Copied', 'تم النسخ') : tr('Share', 'مشاركة')}</button></div></div><div className={`mt-10 space-y-10 ${isArabic ? 'font-sans text-[18px] leading-[2]' : 'font-[Georgia,serif] text-[19px] leading-[1.72]'} text-[#313131] md:text-[21px]`}>{post.sections.map((section, index) => <section key={section.heading.en}>{index === 0 && <p className={`${isArabic ? 'float-right ml-2 mt-[-6px]' : 'float-left mr-2 mt-[-7px]'} ${displayHeading(section.paragraphs[0][language])} text-7xl leading-none text-[#0b3346]`}>{section.paragraphs[0][language].charAt(0)}</p>}<h2 className={`${displayHeading(section.heading[language])} ${index === 0 ? 'sr-only' : 'mt-12'} text-2xl font-bold leading-tight tracking-[-0.025em] text-[#202020] md:text-3xl`}>{section.heading[language]}</h2><div className={index === 0 ? 'space-y-6' : 'mt-5 space-y-6'}>{section.paragraphs.map((paragraph) => <p key={paragraph.en}>{index === 0 && paragraph === section.paragraphs[0] ? paragraph[language].slice(1) : paragraph[language]}</p>)}</div></section>)}</div><div className="mt-14 border-t border-black/10 pt-8"><p className={`${isArabic ? 'font-display' : 'font-[Georgia,serif]'} text-2xl tracking-[-0.03em] text-[#242424]`}>{tr('Enjoyed this note?', 'هل أعجبك هذا المقال؟')}</p><p className="mt-2 text-sm leading-6 text-[#666]">{tr('Follow Ahmed for future writing on Arabic Voice AI, low-latency systems, and applied ML engineering.', 'تابع أحمد لمزيد من الكتابات عن الذكاء الاصطناعي الصوتي العربي والأنظمة منخفضة التأخير وهندسة ML التطبيقية.')}</p><a href="https://www.linkedin.com/in/mrezzat/" target="_blank" rel="noopener noreferrer" className="mt-5 inline-flex items-center gap-2 rounded-full bg-[#0b3346] px-4 py-2.5 text-sm font-semibold text-cyan-50 transition-colors hover:bg-[#0b7f95]"><Linkedin className="h-4 w-4" />{tr('Follow on LinkedIn', 'تابعني على LinkedIn')}</a></div></article></main><footer className="border-t border-black/10 px-6 py-8 text-center text-xs text-[#777]"><Link to={blogUrl} className="hover:text-[#0b7f95]">{tr('More stories in Field Notes', 'مزيد من المقالات في ملاحظات ميدانية')}</Link></footer></div>;
};

export default BlogArticle;
